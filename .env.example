# ==========================================================
# CVE Push Service 环境变量配置示例
# 
# 使用说明：
# 1. 将此文件重命名为 .env
# 2. 填写您的实际配置信息
# 3. 确保 .env 文件已添加到 .gitignore 中，不会被提交到代码仓库
# ==========================================================

# ======================= Server酱配置 =======================
# Server酱3的SendKey，用于接收推送通知
# 获取方法：访问 https://sc3.ft07.com/ 注册账号并获取
SCKEY=您的Server酱SendKey

# ======================= 钉钉配置 =======================
# 钉钉机器人的Webhook地址
# 获取方法：在钉钉群中添加机器人并复制Webhook地址
DINGTALK_WEBHOOK=您的钉钉Webhook地址

# 钉钉机器人的加签密钥（可选）
# 如启用加签功能，请填写该密钥，用于生成签名验证消息来源
# 获取方法：在钉钉群中添加机器人时选择"加签"选项，复制生成的密钥
# DINGTALK_SECRET=您的钉钉加签密钥

# ======================= 邮箱配置 =======================
# SMTP服务器地址（例如：smtp.qq.com、smtp.gmail.com等）
EMAIL_SMTP_SERVER=smtp.example.com

# SMTP服务器端口（通常为587，SSL通常为465）
EMAIL_SMTP_PORT=587

# 发件人邮箱地址
EMAIL_USERNAME=your_email@example.com

# 发件人邮箱密码或授权码（不是登录密码，通常需要在邮箱设置中单独获取）
EMAIL_PASSWORD=您的邮箱授权码或密码

# 收件人邮箱地址（可以与发件人相同）
EMAIL_RECEIVER=receiver@example.com

# ======================= 可选配置 =======================
# 注意：以下配置已在代码中直接设置，如需自定义可以取消注释并修改

# 数据库文件路径（默认为 vulns.db）
# DB_PATH=vulns.db

# 日志文件前缀（默认为 cveflows.log）
# LOG_FILE=cveflows.log

# 数据存储目录（默认为 data）
# DATA_DIR=data

# 只关注CVSS>=7.0的高危漏洞（默认值为7.0）
# CVSS_THRESHOLD=7.0

# 推送模式（可选值：all - 所有推送方式, serverchan - Server酱, dingtalk - 钉钉, email - 邮件）
# 默认值：all
# PUSH_MODE=all